{
  "name": "threejs-logarithmic-solar-system",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A Three.js visualization scaffold for a logarithmically scaled Solar System.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
